# Local production web app dependencies for G-LRAG.
#
# Install with:
#   python -m pip install -r requirements.txt
#
# Use the same Python interpreter you use to run Streamlit.

# Custom UI selectors are verified against this Streamlit DOM version.
streamlit==1.62.0
PyYAML>=6,<7
openai>=3,<4

# Dense FAISS retrieval.
faiss-cpu>=1.8
sentence-transformers>=5.1,<6
transformers>=4.41,<5
huggingface-hub>=0.34,<1
torch>=2.2
rank_bm25>=0.2,<1

# Runtime/data utilities used by the app and evaluation helpers.
numpy>=1.26,<2
pandas>=2,<3
matplotlib>=3.8,<4
fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1

# Test runner for local verification.
pytest>=8,<9
nbformat>=5,<6
