LLM_BASE_URL=https://api.openai.com/v1
LLM_API_KEY=
LLM_BASE_MODEL=gpt-4o-mini
LLM_LARGER_MODEL=gpt-4o

# Optional. Leave this as-is if the graph pickle is stored at data/graph/knowledge_graph.gpickle.
GRAPH_PICKLE_PATH=data/graph/knowledge_graph.gpickle

# Dense-Sparse / BM25. Use the local service in production, or leave
# BM25_SERVICE_URL blank to fall back to a local sparse index in-process.
BM25_SERVICE_URL=http://127.0.0.1:8001
BM25_INDEX_DIR=data/sparse_index
BM25_INDEX_VERSION=local-bm25
BM25_API_KEY=replace-with-random-secret

# Dense-only deployment. Keep faiss for local development; restart UI after changing.
DENSE_BACKEND=faiss
DENSE_SERVICE_URL=http://127.0.0.1:8002
DENSE_API_KEY=
DENSE_TIMEOUT_SECONDS=30
# Server settings (set these on the Pod, not required on the UI host).
DENSE_INDEX_DIR=data/chunk metadata
# Use cpu locally; set cuda on the GPU Pod.
DENSE_DEVICE=cpu
DENSE_EXPECTED_MODEL=intfloat/multilingual-e5-large
DENSE_EXPECTED_INDEX_VERSION=chunk-metadata-faiss-v1
DENSE_EXPECTED_CORPUS_VERSION=pre-processed-v1
DENSE_EXPECTED_DIMENSION=1024

# Default local mode avoids Hugging Face network retries. Set to 0 for the first model download.
HF_HUB_OFFLINE=1

# Keep model/cache downloads on this project drive instead of C:\Users\...
XDG_CACHE_HOME=.cache
HF_HOME=.cache/huggingface
TRANSFORMERS_CACHE=.cache/huggingface/hub
SENTENCE_TRANSFORMERS_HOME=.cache/sentence-transformers
TORCH_HOME=.cache/torch

# Optional. Leave blank to clear a broken inherited proxy while downloading
# Hugging Face models; set a real proxy URL if your network requires one.
HTTP_PROXY=
HTTPS_PROXY=
