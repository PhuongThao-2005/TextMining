% ============================================================================
% Section 5: Implementation and Experimental Setup
% ============================================================================
\section{Implementation and Experimental Setup}
\label{sec:implementation-setup}

This section states the controls and scoring rules needed to interpret the
ablations in Section~\ref{sec:ablation-study}. The benchmark and corpus are
shared where possible, but the five reported analyses are grouped into four
run families and are not one factorial experiment: candidate pools, context
construction, output limits, and latency accounting differ. A result is
therefore compared only with its paired control inside the same family.

\subsection{Pipeline and Frozen Artifacts}
\label{sec:software-architecture}

The evaluated pipeline has five stages: query embedding with
\texttt{intfloat/multilingual-e5-large}; dense FAISS retrieval and, where
enabled, tokenized BM25 retrieval; RRF fusion, graph expansion, or
cross-encoder reranking; serialization of retained chunks into a grounded
prompt; and answer generation and evaluation. The analysis reads frozen
manifests, predictions, aggregate metrics, latency records, and error logs;
it does not rerun models.

Every primary comparison is joined by \texttt{qa\_id}. A valid row has a
prediction, no saved error, and no failed or skipped status. The same ID is
used to join the question category, reference answer, gold chunk set,
ordered retrieved context, output diagnostics, and latency. This row-level
alignment is what makes a reported mean difference traceable to paired cases.

The completed work is organized around five research questions:
\begin{enumerate}
    \item Does document and provision metadata improve exact evidence
    retrieval or document-level coverage?
    \item Does dense--sparse hybrid retrieval improve coverage or rank over
    dense retrieval alone?
    \item Does the current G-LRAG graph policy improve evidence coverage or
    downstream behavior under a ten-chunk context budget?
    \item Which reranker best trades ranking quality against cost when the
    candidate pool is fixed?
    \item Does an explicit chain-of-thought prompt improve observable answer
    behavior when the retrieved context is fixed?
\end{enumerate}

\subsection{Indexes, Graph, and Runtime Settings}
\label{sec:build-process}

The dense/hybrid/graph experiment uses a FAISS index with 1,513,376 chunk
vectors and a graph over the same chunk identifiers. The hybrid branch
searches ten independently built BM25 shards, each with shard-local IDF and
average-document-length statistics; shard candidates are merged by BM25 score
before RRF. Dense and hybrid retrieval use a 50-result search budget, broad
filtering, and a final context of ten chunks; RRF uses $k=60$ when BM25 is
enabled. Graph variants take five seed chunks within their parent provisions,
use \texttt{max\_hop=1}, replace the baseline context, and retain at most ten
graph-context chunks. This family uses broad validity filtering without a
query-time validity overlay or reranking. The associated generation artifacts
use \texttt{gpt-4o-mini}.

The reranker family fixes a 30-chunk RRF candidate pool ($C_{30}$) and a
ten-chunk final context ($T_{10}$). It compares no reranking, RRF-only,
mMiniLM, Qwen3, BGE, and Jina. The primary generator study compares
GPT-4o-mini Base and CoT with the saved top-10 context fixed by question.
The metadata study changes only the indexed string from chunk body to
header-plus-chunk text; encoder, FAISS, top-$k=10$, generator, prompt, seed,
corpus, and benchmark remain fixed.

\begin{table*}[t]
\centering
\scriptsize
\caption{Frozen configurations used by the completed run families. The five
reported analyses include separate retrieval and graph views of the joint
retrieval/graph family. A dash means that the component was disabled.
Candidate/final values differ by family and are not pooled across rows.}
\label{tab:configurations}
\resizebox{\textwidth}{!}{%
\begin{tabular}{lllllll}
\toprule
\textbf{Family} & \textbf{Configuration} & \textbf{Dense text} &
\textbf{Sparse} & \textbf{Graph} & \textbf{Reranking} &
\textbf{Candidate/final} \\
\midrule
Embedding & Chunk-only & chunk text & -- & -- & -- & 10/10 \\
Embedding & Chunk+metadata & header + chunk & -- & -- & -- & 10/10 \\
Retrieval/graph & DenseOnly & header + chunk & -- & -- & -- & 50/10 \\
Retrieval/graph & HybridOnly & header + chunk & BM25 & -- & -- & 50/10 \\
Retrieval/graph & DenseGraph & header + chunk & -- & G-LRAG & -- & 50/10 \\
Retrieval/graph & HybridGraph & header + chunk & BM25 & G-LRAG & -- & 50/10 \\
Reranker & None & shared RRF pool & BM25 & -- & none & 30/10 \\
Reranker & RRF-only & shared RRF pool & BM25 & -- & RRF & 30/10 \\
Reranker & mMiniLM & shared RRF pool & BM25 & -- & mMiniLM CE & 30/10 \\
Reranker & Qwen3 & shared RRF pool & BM25 & -- & Qwen3 & 30/10 \\
Reranker & BGE & shared RRF pool & BM25 & -- & BGE-v2-m3 & 30/10 \\
Reranker & Jina & shared RRF pool & BM25 & -- & Jina-v2 & 30/10 \\
LLM reasoning & GPT-4o-mini Base & fixed context & -- & -- & Base prompt & fixed/10 \\
LLM reasoning & GPT-4o-mini CoT & fixed context & -- & -- & CoT prompt & fixed/10 \\
\bottomrule
\end{tabular}%
}
\end{table*}

\subsection{Benchmark and Metrics}
\label{sec:evaluation-metrics}

The benchmark contains 500 questions: 400 answerable questions with gold
chunk identifiers and 100 unanswerable questions. Retrieval scores use only
the 400 answerable records. Given gold chunks $G_i$ and retrieved top-$k$
chunks $R_{i,k}$,
\begin{equation}
\begin{aligned}
\mathrm{Recall@}k_i&=\frac{|G_i\cap R_{i,k}|}{|G_i|},\\
\mathrm{Hit@}k_i&=\mathbb{1}[G_i\cap R_{i,k}\ne\varnothing].
\end{aligned}
\end{equation}
MRR and nDCG reward an earlier gold rank. Candidate Recall@30 and Candidate
Hit@30 define the evidence ceiling for the reranker family and are identical
across its reranker conditions. Graph runs additionally report document- or
identity-level precision because a graph policy can return fewer chunks.

For a normalized prediction $y_i$ and reference $r_i$, exact match is
\begin{equation}
\mathrm{EM}_i=\begin{cases}1,&y_i=r_i,\\0,&y_i\ne r_i.\end{cases}
\end{equation}
Token F1 uses token overlap $O_i$ and prediction/reference lengths
$L_i^y,L_i^r$:
\begin{equation}
P_i=\frac{O_i}{L_i^y},\qquad R_i=\frac{O_i}{L_i^r},\qquad
F_{1,i}=\frac{2P_iR_i}{P_i+R_i}.
\end{equation}
ROUGE-L uses the same F-score form with the token-level longest common
subsequence in place of $O_i$. These overlap metrics are computed on
successful answerable outputs; they are not legal-correctness scores.

The saved unanswerable score is a template-based answer/refuse diagnostic. In
the retrieval/graph family, unanswerable accuracy is the fraction of the 100
unanswerable outputs containing the Vietnamese fallback phrase
\textit{``Không có đủ thông tin trong ngữ cảnh được cung cấp.''}; it is
distinct from the 500-case decision diagnostic used elsewhere. Citation
presence is the fraction of successful answerable outputs containing at least
one marker, while structural coverage is the fraction of scored factual
sentences with a marker. Neither measure tests claim-to-source entailment.
Completion is successful rows divided by scheduled rows. Latency is reported
in seconds as saved mean, p50, and p95 values where available. Hybrid timing
includes 26.990 seconds per query amortized from 13,495 seconds of BM25 batch
precomputation over 500 questions; it is not a direct online request-latency
measurement. Reranker runs use cached BM25 candidates and therefore also do
not represent full-online sparse latency.

Family-specific preprocessing is retained in the interpretation: the
retrieval/graph notebooks lowercase and collapse whitespace while retaining
punctuation, whereas the reasoning evaluator additionally applies Unicode NFC
normalization and punctuation-to-space replacement. Scores are compared only
within the evaluator that produced them.

\subsection{Pairing, Uncertainty, and Claim Boundaries}
\label{sec:experimental-protocol}

Headline effects are paired by \texttt{qa\_id}. Retrieval deltas use the
shared answerable intersection; scheduled-denominator answerability
diagnostics retain all 500 cases. Percentile paired-bootstrap confidence
intervals use 10,000 resamples (seed 42) for headline metadata,
dense/hybrid, reranker, and GPT comparisons, and 2,000 resamples for the
smaller reranker slices. An interval crossing zero is reported as
inconclusive rather than as evidence of an effect. Exact Match in the primary
GPT pair also uses an exact McNemar test.

The primary GPT-4o-mini Base/CoT runs complete 500/500 cases and have
identical ordered top-10 chunk-ID sequences for every question. DeepSeek,
GLM, and Qwen prompt pairs are descriptive checks: four DeepSeek cases have
different context order, and the Qwen manifests differ in code snapshot,
timeouts, or retry limits. No prompt effect is pooled across models.

Output limits are family-specific: the retrieval/graph runs set no explicit
client-side output-token cap, while the fixed-context generator-reasoning
comparison uses a 1,024-token limit. The graph policy is consequently tested
as a full context-selection change (seed selection plus expansion), not as an
isolated add-on to an otherwise fixed top-10 list.

The artifacts contain no blinded legal-correctness labels, claim-level
faithfulness labels, validated semantic judge, preferred
answer/clarify/refuse labels, raw chain-of-thought, or reasoning-token
accounting. Thus, no result is interpreted as proof of legal correctness,
legal safety, citation entailment, or faithful reasoning merely because it
improves a lexical, rank, citation-format, refusal-format, or latency metric.

\subsubsection{Reproducibility ledger}

Each condition is accompanied by a manifest, per-question output records,
metric summary, and latency file. Derived analyses are versioned with their
source artifacts under \url{ablation_study/} and \url{ablation_work/}; the
reranker candidate reconstruction and paired analysis are in
\url{ablation_work/reranker_choices/analyze_reranker_metrics.py}, while the
generator post-hoc report is in
\url{ablation_study/e2e_LLM_reasoning/results/report.md}. The report reads
these frozen files and does not call a model during analysis.
